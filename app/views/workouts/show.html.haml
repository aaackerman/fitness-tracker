.row
  %h3=@workout.date.strftime("%A, %b %d")

  %table.striped.responsive-table
    %thead
      %tr
        %th Exercise
        %th Performed at
        %th Reps
        %th Weight LBs
        %th Weight KGs
        %th Height

    %tbody
      -@workout.activities.reverse.each do |activity|
        %tr
          %td
            =link_to activity.exercise.name, exercise_path(activity.exercise)
          %td=activity.created_at.strftime("%I:%M:%S %p")
          %td=activity.reps
          %td=activity.weight_lbs
          %td=activity.weight_kgs
          %td=activity.notes
